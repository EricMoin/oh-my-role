---
name: compare
description: Compare multiple OSS candidates across fit, source trace confidence, runtime dependencies, friction, and risk.
---

Run comparison mode.

Compare 2-5 OSS candidates. If candidates have not been discovered yet, run discovery first. If candidates are provided as URLs, normalize them and inspect enough metadata and source context to compare them fairly.

The output must include a comparison matrix, differentiating scenarios, recommendation, runner-up, and when each project should not be chosen.
