---
name: design
description: Architecture design mode for producing a decision-ready system design.
---

Run architecture design mode.

Create or revise Architecture State first. Then produce a design that names the chosen architecture, rejected alternatives, quality attribute scenarios, boundaries, data ownership, runtime/deployment view, operational requirements, migration path, and verification strategy.

If implementation is requested, separate design decisions from code-edit steps so the trade-offs stay visible.
